# SAC-QUTAB Scientific Corrections Closure

> Historical correction record through 2026-09-04. Academy A100/MIG constraints mentioned below were later superseded; the scientific corrections remain evidence, while current operations are defined in [Vast.ai GPU Training Guide](training_vast_ai.md).

## Scope and source

This closes OMP Stage VIII for the frozen post-implementation scientific-review list recovered from `_handoff/OMP_HANDOFF.html` (embedded session record, reviewer `sac-scientific-reviewer-2`). Finding IDs and severities are preserved. No new scientific review was started. “Resolved” below means the pre-training implementation and its local refusal/contract tests satisfy the acceptance criterion; it does not claim EuroSAT, human-review, checkpoint, A100, or empirical results.

## Frozen findings

| ID | Severity | Source and acceptance criterion | Resolution and files | Verification | Status |
|---|---|---|---|---|---|
| B-01 | blocker | Frozen scientific review. Bind checkpoints and the global protocol to the strict manifest/split contract; reject changed seed, fractions, roles, counts, or stale checkpoint data hashes; admit the correct six-run matrix. | `data.py` fully recomputes the split contract; checkpoint v2 stores manifest/split hashes; training, evaluation, preflight, and protocol freeze revalidate them. Evidence: `src/sac_qutab/{data,train,evaluate,governance}.py`, `tests/test_config_data.py`, `tests/test_models_train.py`. | `python -m pytest -q -p no:cacheprovider tests/test_config_data.py tests/test_models_train.py` — 10 passed. Complete suite — 39 passed. | **resolved** |
| B-02 | blocker | Frozen scientific review. Bind semantic review, validation ECDF, calibration detector, config/condition signature, pair manifests, features and detector hyperparameters; recompute eligibility; preserve immutable reviewer display; use independent decisions or document the shared order. | Review freeze v2 validates config, validation-pair hash, immutable displayed fields, thresholds and recomputed condition/class eligibility. ECDF v3 and detector v3 bind checkpoint/run/config/review/pair/feature/fit contracts. Documentation now states that reviewers receive separate sheets in the same deterministic blinded order and make independently attested decisions. Evidence: `src/sac_qutab/{review,evaluate,detector,interventions}.py`, `tests/test_interventions_review.py`, `tests/test_metrics_statistics.py`, README and rationale. | Focused post-edit run: affected review/detector tests passed within the 34 passing tests before two unrelated fixture-test failures; complete suite — 39 passed. | **resolved** |
| B-03 | blocker | Frozen scientific review. Derive projection from the full frozen train split, require warmed timing, include evaluation/cache/export overhead, and apply the team memory cap to aggregate rank reservations. Reject an off-by-one count, cold two-step timing, over-budget projection, and two-rank aggregate cap breach; admit a compliant case. | Original aggregate two-rank behavior was verified before closure. The dated allocation update now rejects two-rank admission, binds one MIG selector, enforces its 18 GiB cap, and adds full-validation timing to the six-run projection while retaining 5+20 optimizer timing, ≤36 total hours, ≥12 recovery hours, and four-hour downstream allowance. Evidence: `src/sac_qutab/{governance,train}.py`, `tests/test_governance.py`. | Original closure suite — 39 passed; MIG-update focused set — 25 passed; updated complete suite — 47 passed. | **resolved** |
| B-04 | blocker | Frozen scientific review. Authenticate the exact pretrained bytes, persist the full digest in model/checkpoint/protocol provenance, and reject mutation/removal in standalone training, run-all, preflight, or protocol freeze. | Weight preflight hashes the complete local file and validates the adapter. Core training now reauthenticates that path and full SHA immediately even on resume. Preflight, run-all, checkpoint v2, and protocol v3 compare the same digest. Evidence: `src/sac_qutab/{models,train,cli,governance,evaluate}.py`, `tests/test_models_train.py`, `tests/test_governance.py`. | `tests/test_models_train.py` passed in the 10-test affected rerun; complete suite — 39 passed. | **resolved** |
| B-05 | blocker | Frozen scientific review. Statistics and reports must consume a real protocol path and authenticate config, evaluations, metrics, representations, detectors, review, and all required artifacts. Mutation or deletion must fail. | Primary statistics requires protocol v3 plus the configured six-run matrix and digest-authenticated evaluation indexes. Reporting now also accepts the loaded `Config`, validates config/condition/metric/statistics contracts and exact run keys, validates the review freeze, verifies every statistics source digest, and refuses incomplete inputs before publication. Evidence: `src/sac_qutab/{statistics,reporting,cli}.py`, `tests/test_evaluation_reporting.py`, `tests/test_metrics_statistics.py`. | Focused statistics/reporting tests passed; complete suite — 39 passed. | **resolved** |
| H-06 | high | Frozen scientific review. Supply original-image-cluster bootstrap intervals for every available absolute RQ3 AUROC/AP score, group the Wilcoxon sensitivity analysis, and expose AUROC, AP, prevalence, interval, state, seed, and class-calibration evidence. | Absolute RQ3 scores are no longer suppressed merely because learned models are unavailable. Each available score gets clustered AP/AUROC with estimate, bounds, cluster count, valid/invalid replicates, confidence, bootstrap state, and seed. Wilcoxon uses one mean effect per `source_group_id`. Reports emit both AP and AUROC, prevalence, detector tables, and condition/class calibration tables/plots using configured ECE bins. Evidence: `src/sac_qutab/{statistics,reporting}.py`, `tests/test_metrics_statistics.py`, `tests/test_evaluation_reporting.py`. | Known perfect-score clustered interval fixture passed; configured six-run reporting fixture passed; complete suite — 39 passed. | **resolved** |
| H-07 | high | Frozen scientific review. Enforce exactly four members, or five/six only with exceptional approval; reject other sizes; require unique assigned/visible GPU IDs and world-size agreement. | Roster rules are unchanged. The v2 allocation requires exactly one unique opaque selector (MIG UUID or numeric selector), exact visible-selector agreement, and world size one; missing/multiple/mismatched selectors and other world sizes fail closed. Evidence: `src/sac_qutab/governance.py`, `tests/test_governance.py`. | Original closure suite — 39 passed; v2 schema/migration/selector/topology checks passed; updated complete suite — 47 passed. | **resolved** |
| H-08 | high | Frozen scientific review. Reconcile README, rationale, runbook, traceability, validation, reviewer order/attestation, full-core timing, DDP, protocol, statistics/reporting schemas, commands, test counts, and artifact names; keep lifecycle mutation guards bounded. | Original reconciliation remains intact. The dated addendum synchronizes `sac-administrative-v2`, `sac-compute-preflight-v2`, one-MIG commands, `compute-mig1.json`, validation-inclusive timing, loader policy, and explicit hardware-unverified language without reopening the review. | MIG-update focused set — 25 passed; complete suite — 47 passed; final scan evidence is in `FINAL_EVIDENCE.md`. | **resolved** |

### 2026-09-04 allocation addendum

The frozen IDs, severities, source criteria, and resolved statuses above were not reinterpreted. The user subsequently reported a different operational allocation: one future 20 GB MIG instance on an A100 40 GB parent, with no current A100 access. Repository evidence therefore supersedes the old two-rank procedure with a fail-closed world-size-one v2 gate. This is an implementation/handoff update, not a new scientific review, and it creates no hardware evidence.

## Training- and external-evidence deferrals

These are not unresolved code corrections. They cannot be completed locally without fabricating evidence.

| Deferred evidence | Why unavailable now | Future procedure and output | Acceptance criterion | Blocks pre-training code readiness? |
|---|---|---|---|---|
| Administrative approval, roster, active window, one-MIG selector/profile/cap, operator/storage record | Requires course/team decisions and the booked host; the allocation is user-reported only. | During future access, observe the UUID/profile, reconcile the newer allocation with the older brief limit, and complete a real private `sac-administrative-v2` JSON. | Strict validator accepts real non-placeholder fields, the roster rule, exactly one selector, A100 parent, 20 GB allocation and 18 GiB operational cap. | No; blocks authorization to execute hardware preflight/training. |
| EuroSAT and grouping evidence | Dataset acquisition was prohibited in this task; real duplicate/geographic structure is unknown. | Manually stage EuroSAT, then run `data-manifest`, `split-create`, and `data-verify`; preserve `artifacts/data/{manifest.jsonl,splits.json}`. | 27,000 valid RGB 64×64 files, ten classes, authenticated grouped roles, no group leakage. No geographic claim without defensible group metadata. | No; blocks real-data execution. |
| Authorized pretrained files | Files were not supplied and downloads were prohibited. | Run `validate-weights` for ResNet18 and DeiT-Tiny; retain both JSON records and exact files. | Adapter/shape checks pass and full SHA-256 matches every later training/preflight/protocol artifact. | No; blocks model timing/training. |
| Qualified human semantic audit | Requires two qualified reviewers and an independent adjudicator. | Run `review-export`; complete separate sheets and attestation; run `review-freeze`; retain review freeze and qualitative panel. | Both pilots usable; 900 final decisions; κ≥0.60; condition≥0.80; each class≥0.60; at least one qualified mild/severe family. | No; blocks protocol/final evaluation. |
| A100 MIG identity/timing/recovery and six trained checkpoints | No A100 access exists now; training and hardware inspection were forbidden. | Later run the exact one-MIG bounded commands in `docs/A100_TRAINING_RUNBOOK.md`, compare workers 4/8 by the frozen rule, create `compute-mig1.json`, complete the resume drill, then invoke single-process `run-all`. | A100 capability 8.0; one assigned selector/world size one; ≥18 GiB visible and ≤18 GiB reserved; 5+20 timing plus bounded validation; total core≤36 h; recovery≥12 h; six authenticated `best.pt` checkpoints. | No for local code readiness; blocks empirical readiness/results. |
| Final-test metrics, multi-seed conclusions, paper/slides/contribution/release | Requires the real freezes and trained checkpoints. | Complete validation→calibration→detector for all six, freeze protocol once, score final test once, run primary statistics and report artifacts, then prepare deliverables. | Complete digest-linked artifacts; honest defined/underpowered states; no fabricated result; course release criteria met. | No for starting A100 work; blocks project/publication completion. |

## Stage VIII disposition

All eight frozen scientific corrections are **resolved for the pre-training codebase**. The remaining items are explicit external/training evidence gates, not incomplete implementation.
